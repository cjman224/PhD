LFE Input 한쪽만, ao0 한쪽만 사용
하여 출력값 넣고,

이는 역시 하나의 액츄에이터로만 출력되게 함.

crossover는 +-버튼 동시에 눌러 제거한 상태.
Volume은 여러 값으로 설정 가능한데,

10과 20, 30을 측정해놓자.


의자위에 올려두고 무게로만 고정된 상태.

가속도계는 정중앙에 테이프로 고정하여 측정.

SensorGain 구하기


sensitivity = 10 mV/g

gain이 20이니깐

측정한 값에 5만 곱하면 됨.




호준 (73kg)몸무게로 깔아앉은 System
가속도계는 actuator 정중앙에 아주 견고하게 붙이고
그위에 깔아앉도록함

Volume을 amp에서 조절가능한데, 10, 20, 30 Level에 맞추고 측정하였음.